{
  "name": "bedrock-test-repo",
  "private": true,
  "version": "0.0.1",
  "description": "Test repository for the Bedrock GitHub App. Realistic Prisma + TypeScript shape so codewalk has real references to find.",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "prisma": "^5.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
